/**
 * @deprecated MVP-era code should import from `./mvp.types` instead.
 * This compatibility shim remains for older SLC-named modules while the
 * codebase migrates naturally under ADR 0018.
 */
export * from './mvp.types';
